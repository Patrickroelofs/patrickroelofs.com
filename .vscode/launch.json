{
  "version": "0.2.0",
  "configurations": [
    {
      "command": "pnpm generate:types",
      "name": "Generate Types CLI",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    },
    {
      "command": "pnpm run dev",
      "name": "Run Dev",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    },
    {
      "command": "pnpm run migrate:create",
      "name": "Create Migration",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
